<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <item android:title="Profile">
        <menu>
            <item
                android:id="@+id/profile"
                android:icon="@drawable/baseline_person_24"
                android:title="Profile"
                />
        </menu>
    </item>

    <item android:title="Home">
        <menu>
            <item
                android:id="@+id/home"
                android:icon="@drawable/round_home_24"
                android:title="home"/>
        </menu>
    </item>

    <item android:title="Logout">
        <menu>
            <item
                android:id="@+id/logout"
                android:icon="@drawable/baseline_logout_24"
                android:title="logout"/>
        </menu>
    </item>

    <item android:title="Requests">
        <menu>
            <item
                android:id="@+id/AllRequest"
                android:icon="@drawable/accepted_request"
                android:title="All Request"/>

            <item
                android:id="@+id/Accepted"
                android:icon="@drawable/accepted_request"
                android:title="accepted"/>

            <item
                android:id="@+id/Rejected"
                android:icon="@drawable/rejected_request"
                android:title="rejected"/>
        </menu>
    </item>

</menu>